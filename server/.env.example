# Server Configuration
PORT=3001
NODE_ENV=development # 'development', 'test', or 'production'

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=ninjaruss
DATABASE_PASSWORD=your_password
DATABASE_NAME=usogui_fansite_db

# Database Safety Controls
ENABLE_SCHEMA_SYNC=true # Set to 'true' in development to enable schema sync, always 'false' in production!
RUN_MIGRATIONS=false # Set to 'true' to automatically run migrations on startup

# JWT Authentication
JWT_SECRET=your_jwt_secret_key_here_make_it_strong_and_unique
JWT_EXPIRES=1d

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:3000

# Email Service (Resend)
RESEND_API_KEY=your_resend_api_key
